<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:showIn="@layout/activity_main"
    tools:context=".MainActivityFragment">

    <TextView android:text="Today, June 24" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/tvToday" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="21°"
        android:id="@+id/tvHighestTemprature"
        android:layout_below="@+id/tvToday"
        android:layout_alignRight="@+id/tvToday"
        android:layout_alignEnd="@+id/tvToday" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="9°"
        android:id="@+id/tvLowestTemprature"
        android:layout_below="@+id/tvHighestTemprature"
        android:layout_alignRight="@+id/tvHighestTemprature"
        android:layout_alignEnd="@+id/tvHighestTemprature" />

    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@mipmap/ic_launcher"
        android:id="@+id/imageView"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/tvToday"
        android:layout_toEndOf="@+id/tvToday" />

</RelativeLayout>
